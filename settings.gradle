/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 * 
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/4.8.1/userguide/multi_project_builds.html
 */

/*import org.apache.tools.ant.filters.ReplaceTokens

loadConfiguration()

def loadConfiguration() {
    def environment = hasProperty('env') ? env : 'and1'
    project.ext.environment = environment
    println "Environment is set to $environment"

    def configFile = file('environment.groovy')
    def config = new ConfigSlurper("$environment").parse(configFile.toURL())
    project.ext.config = config
}

processTestResources {
    filesMatching('device.properties') {
        filter ReplaceTokens, tokens: [
                device: config.device,
                platform: config.platform,
                browser: config.browser,
                server: config.server,
                env: environment
        ]
    }
}
**/
rootProject.name = 'NuevosFlujos'
